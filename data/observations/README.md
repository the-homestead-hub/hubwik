# Observations

v0.1 publishes the row shape, not a public ledger.

See [docs/OBSERVATION.md](../../docs/OBSERVATION.md) and
`../schema/observation.schema.json`.

Do not add partner-identified rows here. `partner_id` is internal and must
never print on a treatise.
