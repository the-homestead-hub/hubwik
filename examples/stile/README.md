# Stile example

A small Cloudflare Worker that answers HTTP 402 to agents and sends browsers to a human treatise.

This is an example. It does not publish a settlement address, a wallet, or a facilitator. Do not deploy it onto a shop checkout hostname.

Copy `wrangler.example.jsonc` to a private deploy overlay, set your own URLs, and keep account identifiers out of this repository.
